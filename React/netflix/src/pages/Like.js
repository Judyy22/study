import React from "react";
import { useSelector } from "react-redux";
import Header from "../component/Header";

const Like = () => {
    return (
        <div>
            <div>
                <Header item={"Coming soon"} />
            </div>
        </div>
    );
};

export default Like;
